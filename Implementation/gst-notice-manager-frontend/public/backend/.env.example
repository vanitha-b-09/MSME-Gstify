# Base URL of your Node/Express backend (no trailing slash)
VITE_API_URL=http://localhost:3000/api

# When "true" the frontend uses an in-memory mock backend (no real API calls).
# Set to "false" once your Node/Express backend is running.
VITE_USE_MOCKS=true
